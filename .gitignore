# ============================================================================
#  产物 / 构建输出（不纳入版本库）
# ============================================================================

# 编译产物：bin/dbxdl，以及运行工具时误放到 bin/ 下的下载结果
/bin/
/dbxdl

# Go 构建缓存（Makefile 通过 GOCACHE 放在项目内，便于沙箱/CI 构建）
/.gocache/

# dbxdl 生成的下载目录与 tar 包
/dbx[0-9]*/
/dbx*.tar*
/dist/

# 未完成的下载分片（正常会在续传后改名，这里兜底）
*.part

# ============================================================================
#  编辑器 / 操作系统
# ============================================================================
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
